# Tiny Python implementation for the Telegram Bot API.

Implements methods:

- getUpdates
- sendMessage
- deleteMessage

Implements types:

- InlineKeyboardMarkup
- ReplyKeyboardMarkup

Added ability to log url request errors to a file and send error messages to the admin in Telegram